<div class="container d-flex justify-content-center align-items-center min-vh-100 bg-dark text-light">
  <div class="d-flex justify-content-between" style="width: 100%; max-width: 1000px; gap: 20px;">
    <div class="card p-5 shadow-lg" style="max-width: 500px; width: 100%; background-color: #343a40; display: flex; align-items: center;">
      <h2 class="text-center mb-4" style="color: #fff;">O que é o <strong>To Do List</strong>?</h2>
      <%= image_tag 'todo-list.webp', alt: 'To Do List', style: 'width: 40%; height: auto; margin-bottom: 20px; border-radius: 10%;' %>
      <p style="color: #fff; text-align: justify">O <b>To Do List</b> é um modelo intuitivo projetado para ajudar você a organizar suas tarefas diárias de maneira <em>prática</em>. Ele permite que você crie, edite e planeje seus afazeres. <em>Registre-se</em> e comece a usar agora mesmo!</p>
    </div>

    <div class="card p-5 shadow-lg" style="max-width: 500px; width: 100%; background-color: #343a40;">
      <h2 class="text-center mb-4">Log in</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "needs-validation" }) do |f| %>
        <div class="mb-3">
          <%= f.label :email, class: "form-label text-light" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control bg-dark text-light border-0" %>
        </div>

        <div class="mb-3">
          <%= f.label :senha, class: "form-label text-light" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control bg-dark text-light border-0" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="mb-3 form-check">
            <%= f.check_box :remember_me, class: "form-check-input" %>
            <%= f.label :Lembrar, class: "form-check-label text-light" %>
          </div>
        <% end %>

        <div class="d-grid">
          <%= f.submit "Entrar", class: "btn btn-primary" %>
        </div>
      <% end %>

      <div class="text-center mt-3">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
